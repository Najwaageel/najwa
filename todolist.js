const button = document.querySelector(".btn")
const text = document.querySelector(".text")

button.addEventListener("click", function ( ) {
    const messages = "Thank you, we will try to add it again ."
    text.innerHTML = messages

})
